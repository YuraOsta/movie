const initialState = {

}

export default function filmListReducer(state = initialState, action){
    switch (action.type) {
        case value:
            
            break;
    
        default:
            return state;
    }
}